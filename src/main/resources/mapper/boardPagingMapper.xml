<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.paging.mapper.BoardPagingMapper">

	<select id="count">
		SELECT
		    COUNT(IDX) AS CNT
		FROM
		    BOARD
		 <where>
	    <if test="boardDto.menu_id != null and boardDto.menu_id != ''">
	      AND MENU_ID = #{ boardDto.menu_id }   
	    </if> 
	    <if test="searchType != null and searchType != '' 
	        and keyword != null and keyword != ''">
	      AND ${searchType}  Like '%' || #{ keyword } || '%'   
	    </if> 	   
	  </where>
	</select>
	
	<select id="getBoardPagingList">
		SELECT
			IDX,
			MENU_ID,
			TITLE,
			WRITER,
			TO_CHAR(REGDATE,'YYYY-MM-DD') REGDATE,
			HIT	
		FROM
			BOARD
		<where>
		<if test="menu_id != null and menu_id != ''"> AND MENU_ID = #{menu_id } </if>
		<if test="searchType != null and searchType != '' and keyword != null and keyword != ''"> AND ${searchType} Like '%' || #{ keyword } || '%' </if>
		</where>
		ORDER BY IDX DESC
		OFFSET #{offset} ROWS FETCH NEXT #{numOfRows} ROWS ONLY
	</select>
	
</mapper>
